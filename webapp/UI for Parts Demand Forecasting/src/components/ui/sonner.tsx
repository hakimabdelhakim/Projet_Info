"use cliété

impété useTheme } from "nétéhemes@0.4.6";
impété Toété as Sonner, ToétéProps } from "sonner@2.0.3";

coétéoété = ({ ...props }: ToétéProps) => {
  coété theme = "sété" } = useTheme();

  étén (
    <Sonner
    étémeétéme as ToétéPropsétéme"]}
      classNameétéster group"
     étée={
        {
          "--normal-bg": "var(--popover)",
          "--normaétét": "var(--popover-foreground)",
          "--normal-border": "var(--border)",
        } as ReétéSSPropétés
      }
      {...props}
    />
  );
};

expétéToété };
