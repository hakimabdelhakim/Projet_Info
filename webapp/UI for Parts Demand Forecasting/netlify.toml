[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

#[ Functions ]
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
